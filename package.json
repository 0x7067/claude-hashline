{
  "name": "claude-hashline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hashline edit tool for Claude Code: replaces str_replace-style editing with line-anchored hashline patches via an MCP server, and blocks the built-in editors.",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "start": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@oh-my-pi/hashline": "15.12.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
