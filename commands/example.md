---
name: example
description: Example slash command — rename or replace with a real one
---

Replace this with the instructions Claude should follow when the user runs
`/example`. Arguments passed to the command are available via `$ARGUMENTS`.
